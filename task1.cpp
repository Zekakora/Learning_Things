#include <iostream>                         // 包含输入输出接口文件
#include <cmath>

// 包含数学函数接口文件
using namespace std;                        // 引用标准命名空间
int main()                                  // 定义主函数
{                                           // 主函数开始
//    cout<<(2 * sin(20.0 / 180.0 * 3.14159) * cos(40.0 / 180.0 * 3.14159) - exp(-2.0)) / (1 + 2 * cos(10.0 / 180.0 * 3.14159) * cos(10.0 / 180.0 * 3.14159))<<endl;
//    cout<<  - 1.0/2.0 + 1.0/3.0 * (2.0/ sqrt(3.0)) - fabs(tan(45.0 / 180.0 * 3.14159) - sqrt(3.0))<<endl;

    double middle = 0;
//    middle = pow(3 ,2);
//    printf("%f", middle);
    cout << 3 * 3 + pow(sqrt(2010) - sqrt(2012), 0) << endl;
    return 0;                               // 函数返回值为0
}                                           // 主函数结束
